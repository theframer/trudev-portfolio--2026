# TRUDEV Portfolio

Personal-brand portfolio built with Next.js, React, TypeScript and CSS.

Run:
npm install
npm run dev

Production check:
npm run lint
npm run build
npm start

The secret unlock API validates TRUDEV_UNLOCK_CODE only on the server. Add a real code in .env.local when the campaign resource is ready.
